// Each #kernel tells which function to compile; you can have many kernels
#pragma kernel CSMain

#include Noise.compute



float4 sampleAt(float3 pos) {
	float4 point_values;

	float spaceVal = -pos.y;

	point_values = (pos, spaceVal);

	return point_values;
}
